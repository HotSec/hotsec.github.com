# 包-模块-库

## 包

- `import "net/http"`
  - 导入完整的模块路径，非包名。
  - 编译器依次搜索标准库、项目根路径，及缓存目录。
  - 引用包成员，用包名，而非导入路径

- 导入多个包

  - ```go
        "osx/lib"
        nix "linux/lib"  别名 nix.xxx
        . "libux/lib"   简化：xxx,相当于from math import *
        _ "math"    初始化：无法引用，仅用来初始化目标包
    ```

## 包管理工具

- 演进历史
  - go get
  - vendor Go1.5
  - dep 2016
  - vgo  2018
    - go mod  Go1.11 Go1.16 GO111MODULE的默认值on
  - go work

## 常用命令

- `go env -w GOPROXY=https://goproxy.cn,direct`
- go mod
  - `replace github.com/gin-gonic/gin => ../gin` 将依赖的gin框架替换为 本地路径中的代码
  - go mod vendor # 将依赖复制到./vendor目录下
- go work # 解决多模块开发可能遇到的问题
  - go work init # 初始化工作空间
  - go work use ./xxx1 ./xxx2 # 添加模块
  - GOWORK=off go build -mod vendor # 禁用工作空间，以vendor模式编译
  - go work sync # 同步工作空间依赖
- GOPRIVATE
  - go env -w GOPRIVATE="*.example.com"

- GOPATH
