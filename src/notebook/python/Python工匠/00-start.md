# Python工匠

- [1. 变量与注释](#1-变量与注释)
  - [1.1. 给变量注明类型](#11-给变量注明类型)
  - [1.2. 变量命名原则](#12-变量命名原则)
  - [1.3. 注释](#13-注释)
  - [1.4. 一个冒泡排序的例子](#14-一个冒泡排序的例子)
  - [1.5. 变量定义尽量靠近使用](#15-变量定义尽量靠近使用)
  - [1.6. 定义临时变量提升可读性](#16-定义临时变量提升可读性)
  - [1.7. 同一作用域内不要有太多变量](#17-同一作用域内不要有太多变量)
  - [1.8. 能不定义变量就别定义](#18-能不定义变量就别定义)
  - [1.9. 不要使用locals()](#19-不要使用locals)
  - [1.10. 空行也是一种注释](#110-空行也是一种注释)
  - [1.11. 先写注释，后写代码](#111-先写注释后写代码)
  - [1.12. 小结](#112-小结)
- [数值与字符串](#数值与字符串)
- [3. 容器: list、tuple、dict、set等](#3-容器-listtupledictset等)
- [4. 边界处理](#4-边界处理)

## 1. 变量与注释

```py
## 去掉 s 两边的空格，再处理
# value = process(s.strip()) # bad

# 用户输入可能会有空格，使用 strip 去掉空格
username = extract_username(input_string.strip())  # good
```

```py
# 交换两个变量
author, reader = "zmf", "zmf96"

author, reader = reader, auther

# 变量解包
usernames = ["zmf", "zmf96"]
author, reader = usernames

# 星号表达式（*variables）作为变量名，它便会贪婪[1]地捕获多个值对象，并将捕获到的内容作为列表赋值给 variables
>>> data = ['piglei', 'apple', 'orange', 'banana', 100]
>>> username, *fruits, score = data
>>> username
'piglei'
>>> fruits
['apple', 'orange', 'banana']
>>> score
100

# 变量解包可以在循环里用
>>> for username, score in (['piglei', 100], ['raymond', 60]):
...     print(username)
...
piglei
raymond
```

单下划线变量名_
    ps: 在python交互式环境中`_`默认保存着你输入的上个表达式的返回值

### 1.1. 给变量注明类型

类型注解
Sphinx格式文档

```py
def remove_invalid(items):
    """剔除 items 里面无效的元素

    :param items: 待剔除对象
    :type items: 包含整数的列表，[int, ...]
    """
```

### 1.2. 变量命名原则

- pep8原则
  - 4个空格缩进
  - 每行不超过79个字符
  - 普通变量: 蛇形命令 max_value
  - 常量： 全大写字母：MAX_VALUE
  - 标记仅内部使用，增加下划线前缀：_local_var
  - 当名字与Python关键字冲突时，末尾追加下划线: class_
  - 类名：FooClass
  - 函数：bar_function
- 描述性要强
- 要尽量短
- 要匹配类型
  - 最好别拿一个名词的复数形式来作为int类型的变量名，如apples会与list(apple)混淆
- 超短命名
  - i、j、k
  - n
  - s
  - e
  - fp

### 1.3. 注释

- 不要用注释屏蔽代码
- 注释复述代码
- 接口注释不要写实现细节，着重描述函数的功能、参数说明等

### 1.4. 一个冒泡排序的例子

```py
def magic_bubble_sort(numbers: List[int]):
    """有魔力的冒泡排序算法，默认所有的偶数都比奇数大

    :param numbers: 需要排序的列表，函数将会直接修改原始列表
    """
    stop_position = len(numbers) - 1
    while stop_position > 0:
        for i in range(stop_position):
            current, next_ = numbers[i], numbers[i + 1] 
            current_is_even, next_is_even = current % 2 == 0, next_ % 2 == 0
            should_swap = False

            # 交换位置的两个条件：
            # - 前面是偶数，后面是奇数
            # - 前面和后面同为奇数或者偶数，但是前面比后面大
            if current_is_even and not next_is_even:
                should_swap = True
            elif current_is_even == next_is_even and current > next_:
                should_swap = True

            if should_swap:
                numbers[i], numbers[i + 1] = numbers[i + 1], numbers[i]
        stop_position -= 1
    return numbers
```

### 1.5. 变量定义尽量靠近使用

### 1.6. 定义临时变量提升可读性

### 1.7. 同一作用域内不要有太多变量

### 1.8. 能不定义变量就别定义

### 1.9. 不要使用locals()

### 1.10. 空行也是一种注释

### 1.11. 先写注释，后写代码

### 1.12. 小结

- 变量和注释决定“第一印象”：
  - 变量和注释是代码里最接近自然语言的东西，它们的可读性非常重要
  - 即使是实现同一个算法，变量和注释不一样，代码给人的感觉会截然不同

- 基础知识：
  - Python 的变量赋值语法非常灵活，可以使用 *variables 星号表达式灵活赋值

- 编写注释的两个要点：
  - “不要用来屏蔽代码”以及“解释为什么”
  - 接口注释是为使用者而写，因此接口注释应该简明扼要的描述函数职责，而不必包含太多内部细节
  - 你可以用 Sphinx 格式文档或类型注解给变量标明类型

- 变量名字很重要：
  - 给变量起名要遵循 PEP 8 指南，代码的其他部分也同样如此
  - 尽量给变量起描述性强的名字，但评价描述性也需要结合场景
  - 在保证描述性的前提下，变量名要尽量短
  - 变量名要匹配它所表达的类型
  - 你可以使用一两个字母的超短名字，但注意不要过度

- 代码组织技巧：
  - 按照代码的职责来组织代码：让变量定义靠近引用
  - 适当定义临时变量可以提升代码可读性
  - 但不必要的变量反而会让代码显得冗长、啰嗦
  - 同一个作用域内不要有太多变量，解决办法：提炼数据类、拆分函数
  - 空行也是一种特殊的“注释”，适当的空行可以让代码更好读
  - 代码可维护性技巧：
  - 保持变量在两个方面的一致性：名字一致性与类型一致性
  - 显式优于隐式：不要使用 locals() 批量获取变量
  - 把接口注释当成一种函数设计工具，先写注释，后写代码

## 数值与字符串

最佳实践

  1. 少写数字字面量
       - 使用 enum 枚举类型改善代码
  2. 别在裸字符串处理上走太远
  3. 不必预计算字面量表达式
     1. python解释器会自动优化，对性能没有影响，而且会增加代码的可读性

实用技巧

  1. 布尔值其实也是“数字”
  2. 改善超长字符串的可读性
       - 当多级缩进里出现多行字符串时 textwrap.dedent
       - 大数字也可以变得更加可读 `10_000_000.0、0xABCD_DAAB`
  3. 别忘了那些 “r” 开头的内建字符串函数
  4. 使用“无穷大” float("inf")

常见误区

   1. “value += 1” 并非线程安全
   2. 字符串拼接并不慢

结语：

- 编写代码时，请考虑阅读者的感受，不要出现太多神奇的字面量
- 当操作结构化字符串时，使用对象化模块比直接处理更有优势
- dis 模块非常有用，请多多使用它验证你的猜测
- 多线程环境下的编码非常复杂，要足够谨慎，不要相信自己的直觉
- Python 语言的更新非常快，不要被别人的经验所左右

## 3. 容器: list、tuple、dict、set等

## 4. 边界处理

- 使用条件判断和异常捕获都可以用来处理边界情况
- 在 Python 里，我们更倾向于使用基于异常捕获的 EAFP 风格
- 使用 defaultdict / setdefault / pop 可以巧妙的处理当键不存在时的边界情况
- 对列表进行不存在的范围切片不会抛出异常
- 使用 `or` 可以简化默认值边界处理逻辑，但也要注意不要掉入陷阱
- 不要手动去做数据校验，使用 `pydantic` 或其他的数据校验模块
- 利用取模、绝对值计算等方式，可以简化一些特定的边界处理逻辑
