# 如何设计一个高并发系统

- [1. 横向扩展](#1-横向扩展)
- [2. 接口常规优化](#2-接口常规优化)
  - [2.1. 批量思想](#21-批量思想)
  - [2.2. 异步思想](#22-异步思想)
  - [2.3. 空间换时间-恰当的使用缓存](#23-空间换时间-恰当的使用缓存)
  - [2.4. 预取思想-提前初始化到缓存](#24-预取思想-提前初始化到缓存)
  - [2.5. 池化思想-预分配与循环使用](#25-池化思想-预分配与循环使用)
  - [2.6. 事件回调思想-避免阻塞](#26-事件回调思想-避免阻塞)
  - [2.7. 远程调用-串行改并行](#27-远程调用-串行改并行)
  - [2.8. 锁粒度-减小锁的粒度](#28-锁粒度-减小锁的粒度)
  - [2.9. 切换存储方式-文件中转暂存数据](#29-切换存储方式-文件中转暂存数据)
  - [2.10. 索引](#210-索引)
    - [2.10.1. 索引不生效的场景](#2101-索引不生效的场景)
    - [2.10.2. 索引设计不合理](#2102-索引设计不合理)
  - [2.11. 优化SQL](#211-优化sql)
    - [2.11.1. mysql慢查询的原因](#2111-mysql慢查询的原因)
    - [2.11.2. 高质量SQL的建议](#2112-高质量sql的建议)
  - [2.12. 避免大事务问题](#212-避免大事务问题)
  - [2.13. 深分页问题](#213-深分页问题)
    - [2.13.1. 标签记录法](#2131-标签记录法)
    - [2.13.2. 延迟关联法](#2132-延迟关联法)
  - [2.14. 优化程序结构](#214-优化程序结构)
  - [2.15. 压缩传输内容](#215-压缩传输内容)
  - [2.16. 海量数据处理，考虑使用NoSQL](#216-海量数据处理考虑使用nosql)
  - [2.17. 线程池设计要合理（协程池）](#217-线程池设计要合理协程池)
  - [2.18. 机器问题](#218-机器问题)
- [3. 微服务拆分](#3-微服务拆分)
- [4. 分库分表](#4-分库分表)
- [5. 池化技术](#5-池化技术)
- [6. 主从分离](#6-主从分离)
- [7. 使用缓存](#7-使用缓存)
- [8. CDN](#8-cdn)
- [9. 消息队列](#9-消息队列)
- [10. ElasticSearch](#10-elasticsearch)
- [11. 降级熔断](#11-降级熔断)
- [12. 限流](#12-限流)
- [13. 异步](#13-异步)
- [14. 压测](#14-压测)
- [15. 突发流量应对](#15-突发流量应对)
- [16. 高并发rpc框架](#16-高并发rpc框架)
- [17. 50wQPS下设计未读数系统](#17-50wqps下设计未读数系统)

## 1. 横向扩展

## 2. 接口常规优化

### 2.1. 批量思想

- 批量操作数据库

### 2.2. 异步思想

- 耗时操作，考虑放到异步执行
  - 线程池
  - 消息队列

### 2.3. 空间换时间-恰当的使用缓存

- redis缓存
- Map缓存

### 2.4. 预取思想-提前初始化到缓存

### 2.5. 池化思想-预分配与循环使用

### 2.6. 事件回调思想-避免阻塞

### 2.7. 远程调用-串行改并行

### 2.8. 锁粒度-减小锁的粒度

### 2.9. 切换存储方式-文件中转暂存数据

- eg: 如果你的接口耗时瓶颈就在数据库插入操作这里，用来批量操作等，还是效果还不理想，就可以考虑用文件或者MQ等暂存。有时候批量数据放到文件，会比插入数据库效率更高。

### 2.10. 索引

- 添加索引是成本最小的优化
  - sql加索引了吗
  - 索引是否真的生效
  - 索引是否合理
- 一般是sql的where的条件字段或者order by、group by后面的字段需要添加索引

#### 2.10.1. 索引不生效的场景

- 隐式的类型转换，索引失效
- 查询条件包含or,可能导致索引失效
- like通配符可能导致索引失效
- 查询条件不满足联合索引的最左匹配原则
- 在索引列上使用mysql的内置函数
- 对索引进行列运算（如，+、-、*、/）,索引不生效
- 索引字段上使用（！= 或者 < >），索引可能失效
- 索引字段上使用is null， is not null，索引可能失效
- 左右连接，关联的字段编码格式不一样
- 优化器选错了索引

#### 2.10.2. 索引设计不合理

- 删除冗余和重复索引
- 索引一般不能超过5个
- 索引不适合建在有大量重复数据的字段上、如性别字段
- 适当使用覆盖索引
- 如果需要使用force index强制走某个索引，那就需要考虑索引设计是否合理

### 2.11. 优化SQL

- select具体字段，避免select *
- 多用limit
- 尽量用union all 代替union
- 优化group by
- 优化order by
- 小表驱动大表
- 字段类型使用合理
- 优化litmit分页
- exist & in的合理使用
- join关联的表不宜过多
- delete+in子查询不走索引
- in元素不要过多

#### 2.11.1. mysql慢查询的原因

- sql没加索引
- sql索引没生效
- limit深分页问题
- 单表数据量大
- join或子查询过多
- in元素过多（不要超过500）
- 数据库在刷脏页
- order by 文件排序
- 拿不到锁
  - show processlist 查看当前语句状态
- delete + in查询不走索引
- group by使用临时表
- 系统硬件或者网络资源

#### 2.11.2. 高质量SQL的建议

### 2.12. 避免大事务问题

- RPC远程调用不要放到事务里面
- 一些查询相关的操作，尽量放到事务之外
- 事务中避免处理太多数据

### 2.13. 深分页问题

- mysql的分页查询，如果数据量很大，limit 1000000, 10，这种查询会非常慢

#### 2.13.1. 标签记录法

- 每次查询时，先查询上一次查询的结束位置，然后从这个位置开始查询 where id > 1000000 limit 10

#### 2.13.2. 延迟关联法

- 先查询主键索引，然后再关联查询 where id in (select id from table where id > 1000000 limit 10)
- eg: select  acct1.id,acct1.name,acct1.balance FROM account acct1 INNER JOIN (SELECT a.id FROM account a WHERE a.create_time > '2020-09-19' limit 100000, 10) AS acct2 on acct1.id= acct2.id;

### 2.14. 优化程序结构

- eg: 会员用户第一次登录，发送感谢消息
  - isFristLogin && isUserVip 比 isUserVip && isFristLogin 性能更好,后者的判断会更少

### 2.15. 压缩传输内容

- 压缩传输内容，传输报文变得更小，因此传输会更快啦。10M带宽，传输10k的报文，一般比传输1M的会快

### 2.16. 海量数据处理，考虑使用NoSQL

- 如果你的数据量很大，比如千万级别，亿级别或者要统计和模糊搜索，那么mysql可能就不够用了，这时候可以考虑使用NoSQL，比如mongoDB、Elasticearch、hbase等

### 2.17. 线程池设计要合理（协程池）

- 关注这几个参数：核心线程、最大线程数量、阻塞队列。
  - 如果核心线程过小，则达不到很好的并行效果。
  - 如果阻塞队列不合理，不仅仅是阻塞的问题，甚至可能会OOM
  - 如果线程池不区分业务隔离，有可能核心业务被边缘业务拖垮。

- 线程池大小设计要合理
  - 线程池大小 = CPU核数 * (1 + 平均等待时间/平均计算时间)
- 线程池任务队列大小设计要合理
  - 队列大小 = CPU核数*(1 + 平均等待时间/平均计算时间)*2

### 2.18. 机器问题

- 内存问题: fullGC、OOM
- 线程打满
- 太多IO资源没关闭
- 磁盘io太慢（虚拟机经常遇到）

## 3. 微服务拆分

- 把一个单体应用拆分成多个服务模块看

## 4. 分库分表

- 分库：跟多的并发支持，更多的磁盘容量
- 分表：单个表的查询性能

## 5. 池化技术

- 复用连接：数据库连接池、redis连接池、http连接池
- ants协程池

## 6. 主从分离

- 一般单台msql: tps 500、 qps 1w

- 实时性要求不高的读请求都去读从库
- 写请求或实时性高的走主库
  
## 7. 使用缓存

- redis缓存
- 本地缓存

## 8. CDN

- 加速静态资源访问

## 9. 消息队列

- 削峰
- 活动高峰避免流量暴涨，打垮系统

## 10. ElasticSearch

- 一般搜索功能会用到
- es扩容方便,天然支持高并发,当数据量大时不用动不动就加机器扩容、分库等。

## 11. 降级熔断

- 避免服务雪崩
  - 分布式系统某个基础服务不可用，最终导致整个服务不可用
- 当下级服务出问题时，打开开关降级，不再调用下游系统

## 12. 限流

- 标准库 golang.org/x/time/rate
  - 基于令牌桶算法
- uber-go/ratelimit 基于令牌桶算法
- Gin框架中间件
- 动态限流/自适应限流
- 分布式限流
  - 基于redis
  - nginx限流

- 限流算法
  - 计数器算法
  - 滑动窗口算法
  - 漏桶算法
  - 令牌桶算法
  - 滑动日志

## 13. 异步

- 在合适的场景使用异步
  - 异步任务：消息队列、定时任务、延时任务
  - 异步IO：epoll、select、poll、kqueue、iocp

## 14. 压测

- 全链路压力测试

- 压测工具
  - wrk
  - ab
  - go-stress
  - locust
  - jmeter
  - golang.org/x/perf/cmd/benchstat
- 压测指标
  - tps
  - qps
  - rps
  - p99
  - p95

## 15. 突发流量应对

- 降级、限流、异步保证系统不挂
- 扩容：增加从库、提升配置的方式，提升系统/组件的流量承载能力
- 切流量：服务多机房部署，大流量来了，把流量切到其他机房
  - 全步流量经过一个机房入口，在入口下面的某个负载均衡层转到另外一个机房
    - 依赖专线
  - 域名解析，流量的最前端切换
    - 配置某些地区或者全部地方的一定百分比的流量切换到另外一个机房
      - dns缓存,切换再小时级别

## 16. 高并发rpc框架

- 选择高性能的 I/O 模型, eg: 同步多路 I/O 复用模型
- 调试网络参数，这里面有一些经验值的推荐。比如将 tcp_nodelay 设置为 true,也有一些参数需要在运行中来调试,比如接受缓冲区和发送缓冲区的大小,客户端连接请求缓冲队列的大小(back log)等等；
- 序列化协议依据具体业务来选择。如果对性能要求不高可以选择 JSON,否则可以从 Thrift 和 Protobuf 中选择其一。

## 17. 50wQPS下设计未读数系统

- 评论未读、@未读、赞未读等一对一关系的未读数可以使用上节课讲到的通用计数方案来解决；
  - 数据库 + 缓存的方案是计数系统的初级阶段，完全可以支撑中小访问量和存储量的存储服务。如果你的项目还处在初级阶段，量级还不是很大，那么你一开始可以考虑使用这种方案。
  - 通过对原生 Redis 组件的改造，我们可以极大地减小存储数据的内存开销。
  - 使用 SSD+ 内存的方案可以最终解决存储计数数据的成本问题。这个方式适用于冷热数据明显的场景，你在使用时需要考虑如何将内存中的数据做换入换出。
- 在系统通知未读、全量用户打点等存在有限的共享存储的场景下，可以通过记录用户上次操作的时间或者偏移量，来实现未读方案；
- 信息流未读方案最为复杂，采用的是记录用户博文数快照的方式

