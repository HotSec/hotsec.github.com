# 3.11

## 异步任务组

```python
async def main():
    async with asyncio.TaskGroup() as tg:
        tg.create_task(task1())
        tg.create_task(task2())
    # 自动等待所有任务完成

asyncio.run(main())
```

## f-string中使用=进行调试输出

## 类型注解增强

```python
class MyClass:
    def method(self) -> Self:
        return self  # 明确表示返回当前类的实例
```

