# 在线文档多人实时编辑实现

## 1. 核心问题

多人同时编辑同一份 Markdown 文档，需要解决**并发冲突**。方案：**Yjs CRDT**（前端冲突解决）+ **Golang WebSocket Hub**（消息中转）+ **Redis Pub/Sub**（多实例同步）+ **PostgreSQL**（持久化）。

## 2. 算法选型：CRDT

### OT vs CRDT

| 维度 | OT（操作转换） | CRDT（无冲突复制数据类型） |
|------|---------------|--------------------------|
| 中心化 | 必须有中心服务器 | 去中心化，可离线 |
| 实现难度 | 变换函数复杂，易出错 | 数学保证收敛，库成熟 |
| 扩展性 | 单点瓶颈 | 水平扩展 |
| 代表 | Google Docs（早期） | Figma、Yjs、Automerge |

**选择 Yjs CRDT**：前端用 `Y.Text` 处理冲突，后端只做消息转发，无需理解文档内容。

### Yjs 核心概念

```
Y.Doc → 文档实例（含多个共享类型）
Y.Text → 文本序列（用于 Markdown 内容）
Awareness → 临时状态（光标、选区、在线用户）
```

- 操作满足**交换律 + 结合律**，多个副本独立修改后自动收敛
- 通过二进制编码（`Y.encodeStateAsUpdate`）高效传输
- 支持 `y-indexeddb` 离线持久化，重连自动合并

## 3. 系统架构

```
┌─────────────┐    WebSocket     ┌──────────────────┐
│  浏览器 A    │◄───────────────►│                  │
│  CodeMirror  │                 │  Golang          │
└─────────────┘                 │  WebSocket Hub   │
                                │                  │
┌─────────────┐    WebSocket     │  ┌────────────┐  │
│  浏览器 B    │◄───────────────►│  │ Room 管理   │  │
│  CodeMirror  │                 │  │ 认证鉴权    │  │
└─────────────┘                 │  │ 定时持久化  │  │
                                │  └─────┬──────┘  │
                                └────────┼─────────┘
                                         │
                          ┌──────────────┼──────────────┐
                          ▼              ▼              ▼
                    ┌──────────┐  ┌──────────┐  ┌──────────┐
                    │ PostgreSQL│  │ Redis    │  │ S3/OSS   │
                    │ 文档快照  │  │ Pub/Sub  │  │ 图片附件  │
                    └──────────┘  └──────────┘  └──────────┘
```

### 数据流

```
用户输入 → CodeMirror → Yjs Y.Text → WebSocket → Golang Hub
                                                       │
                                                广播给同 Room 用户
                                                       │
                                                定时/阈值 → PG 持久化
```

## 4. 前端实现（CodeMirror 6 + Yjs）

CodeMirror 6 原生支持 Markdown，配合 `y-codemirror.next` 实现协作。

### 4.1 安装

```bash
npm install yjs y-websocket y-protocols y-indexeddb
npm install @codemirror/state @codemirror/view @codemirror/lang-markdown
npm install codemirror @codemirror/theme-one-dark
npm install y-codemirror.next
```

### 4.2 协作编辑器组件（React）

```tsx
import { useEffect, useRef } from 'react'
import { EditorView, keymap } from '@codemirror/view'
import { EditorState } from '@codemirror/state'
import { markdown } from '@codemirror/lang-markdown'
import { oneDark } from '@codemirror/theme-one-dark'
import { defaultKeymap, historyKeymap, indentWithTab } from '@codemirror/commands'
import { basicSetup } from 'codemirror'
import * as Y from 'yjs'
import { WebsocketProvider } from 'y-websocket'
import { yCollab } from 'y-codemirror.next'
import { IndexeddbPersistence } from 'y-indexeddb'
import { UndoManager } from 'yjs'

interface Props {
  docId: string
  user: { name: string; color: string }
}

export function CollabEditor({ docId, user }: Props) {
  const ref = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const ydoc = new Y.Doc()
    const ytext = ydoc.getText('content')
    const undoManager = new UndoManager(ytext)
    const provider = new WebsocketProvider(
      `ws://${location.host}/ws`, docId, ydoc,
      { params: { token: getToken() } }
    )
    new IndexeddbPersistence(docId, ydoc) // 离线持久化

    const state = EditorState.create({
      extensions: [
        basicSetup,
        markdown(),
        oneDark,
        keymap.of([...defaultKeymap, ...historyKeymap, indentWithTab]),
        yCollab(ytext, provider.awareness, { undoManager }),
      ],
    })

    const view = new EditorView({ state, parent: ref.current! })
    return () => { view.destroy(); provider.destroy(); ydoc.destroy() }
  }, [docId])

  return <div ref={ref} className="h-full" />
}
```

### 4.3 在线用户列表

```tsx
function OnlineUsers({ awareness }: { awareness: any }) {
  const [users, setUsers] = useState<{ name: string; color: string }[]>([])

  useEffect(() => {
    const update = () => {
      setUsers(
        [...awareness.getStates().values()]
          .map((s: any) => s.user)
          .filter(Boolean)
      )
    }
    awareness.on('change', update)
    update()
    return () => awareness.off('change', update)
  }, [awareness])

  return (
    <div className="flex -space-x-2">
      {users.map((u, i) => (
        <div key={i} title={u.name}
          className="w-7 h-7 rounded-full border-2 border-white
                     flex items-center justify-center text-white text-xs"
          style={{ backgroundColor: u.color }}>
          {u.name[0]}
        </div>
      ))}
    </div>
  )
}
```

## 5. Golang 后端实现

### 5.1 项目结构

```
collab-server/
├── main.go
├── hub.go          # Room 管理 + 消息广播
├── client.go       # 单个 WebSocket 连接
├── store.go        # PostgreSQL 持久化
├── go.mod
└── go.sum
```

### 5.2 go.mod

```
module collab-server

go 1.22

require (
    github.com/gorilla/websocket v1.5.3
    github.com/lib/pq v1.14.0
    github.com/redis/go-redis/v9 v9.7.0
    github.com/golang-jwt/jwt/v5 v5.2.1
)
```

### 5.3 main.go

```go
package main

import (
	"log"
	"net/http"
	"os"

	"github.com/golang-jwt/jwt/v5"
)

func main() {
	hub := NewHub(getRedis(), NewPGStore(os.Getenv("DATABASE_URL")))
	go hub.Run()

	http.HandleFunc("/ws", authMiddleware(func(w http.ResponseWriter, r *http.Request) {
		docID := r.URL.Query().Get("doc")
		if docID == "" {
			http.Error(w, "missing doc", http.StatusBadRequest)
			return
		}
		user := r.Context().Value("user").(string)
		ServeWS(hub, w, r, docID, user)
	}))

	log.Println("listening :8080")
	log.Fatal(http.ListenAndServe(":8080", nil))
}

func authMiddleware(next http.HandlerFunc) http.HandlerFunc {
	secret := []byte(os.Getenv("JWT_SECRET"))
	return func(w http.ResponseWriter, r *http.Request) {
		tokenStr := r.URL.Query().Get("token")
		t, err := jwt.Parse(tokenStr, func(t *jwt.Token) (any, error) { return secret, nil })
		if err != nil || !t.Valid {
			http.Error(w, "unauthorized", http.StatusUnauthorized)
			return
		}
		ctx := context.WithValue(r.Context(), "user", t.Claims.(jwt.MapClaims)["sub"])
		next(w, r.WithContext(ctx))
	}
}
```

### 5.4 hub.go — Room 管理与消息广播

```go
package main

import (
	"context"
	"sync"

	"github.com/redis/go-redis/v9"
)

type Hub struct {
	mu       sync.RWMutex
	rooms    map[string]map[*Client]struct{}
	rdb      *redis.Client
	store    *PGStore
}

func NewHub(rdb *redis.Client, store *PGStore) *Hub {
	return &Hub{
		rooms: make(map[string]map[*Client]struct{}),
		rdb:   rdb,
		store: store,
	}
}

func (h *Hub) Run() {
	ctx := context.Background()
	pubsub := h.rdb.Subscribe(ctx, "collab:*")
	ch := pubsub.Channel()
	for msg := range ch {
		data := []byte(msg.Payload)
		docID := msg.Channel[len("collab:"):]
		h.mu.RLock()
		for c := range h.rooms[docID] {
			select {
			case c.send <- data:
			default:
			}
		}
		h.mu.RUnlock()
	}
}

func (h *Hub) Join(docID string, c *Client) {
	h.mu.Lock()
	if h.rooms[docID] == nil {
		h.rooms[docID] = make(map[*Client]struct{})
	}
	h.rooms[docID][c] = struct{}{}
	h.mu.Unlock()
}

func (h *Hub) Leave(docID string, c *Client) {
	h.mu.Lock()
	if room, ok := h.rooms[docID]; ok {
		delete(room, c)
		if len(room) == 0 {
			delete(h.rooms, docID)
		}
	}
	h.mu.Unlock()
}

func (h *Hub) Broadcast(docID string, data []byte, from *Client) {
	h.rdb.Publish(context.Background(), "collab:"+docID, string(data))
}
```

### 5.5 client.go — WebSocket 连接处理

```go
package main

import (
	"log"
	"time"

	"github.com/gorilla/websocket"
)

const (
	writeWait      = 10 * time.Second
	pongWait       = 60 * time.Second
	pingPeriod     = (pongWait * 9) / 10
	snapshotEvery  = 50
)

type Client struct {
	hub  *Hub
	conn *websocket.Conn
	send chan []byte
	doc  string
	user string
	ops  int
}

var upgrader = websocket.Upgrader{CheckOrigin: func(r *http.Request) bool { return true }}

func ServeWS(hub *Hub, w http.ResponseWriter, r *http.Request, docID, user string) {
	conn, err := upgrader.Upgrade(w, r, nil)
	if err != nil {
		log.Println(err)
		return
	}

	// 发送历史快照
	if state, err := hub.store.Load(r.Context(), docID); err == nil && state != nil {
		conn.WriteMessage(websocket.BinaryMessage, state)
	}

	c := &Client{hub: hub, conn: conn, send: make(chan []byte, 256), doc: docID, user: user}
	hub.Join(docID, c)

	go c.writePump()
	go c.readPump()
}

func (c *Client) readPump() {
	defer func() { c.conn.Close(); c.hub.Leave(c.doc, c) }()
	c.conn.SetReadLimit(1 << 20) // 1MB
	c.conn.SetReadDeadline(time.Now().Add(pongWait))
	c.conn.SetPongHandler(func(string) error { c.conn.SetReadDeadline(time.Now().Add(pongWait)); return nil })

	for {
		_, msg, err := c.conn.ReadMessage()
		if err != nil {
			break
		}
		c.hub.Broadcast(c.doc, msg, c)
		c.ops++
		if c.ops%snapshotEvery == 0 {
			go c.hub.store.Save(context.Background(), c.doc, msg)
		}
	}
}

func (c *Client) writePump() {
	ticker := time.NewTicker(pingPeriod)
	defer func() { ticker.Stop(); c.conn.Close() }()

	for {
		select {
		case msg, ok := <-c.send:
			c.conn.SetWriteDeadline(time.Now().Add(writeWait))
			if !ok {
				c.conn.WriteMessage(websocket.CloseMessage, []byte{})
				return
			}
			c.conn.WriteMessage(websocket.BinaryMessage, msg)
		case <-ticker.C:
			c.conn.SetWriteDeadline(time.Now().Add(writeWait))
			if err := c.conn.WriteMessage(websocket.PingMessage, nil); err != nil {
				return
			}
		}
	}
}
```

### 5.6 store.go — PostgreSQL 持久化

```go
package main

import (
	"context"
	"database/sql"
	"time"

	_ "github.com/lib/pq"
)

type PGStore struct {
	db *sql.DB
}

func NewPGStore(dsn string) *PGStore {
	db, err := sql.Open("postgres", dsn)
	if err != nil {
		panic(err)
	}
	db.Exec(`CREATE TABLE IF NOT EXISTS documents (
		id TEXT PRIMARY KEY,
		state BYTEA,
		updated_at TIMESTAMPTZ DEFAULT NOW()
	)`)
	return &PGStore{db: db}
}

func (s *PGStore) Save(ctx context.Context, docID string, state []byte) error {
	_, err := s.db.ExecContext(ctx,
		`INSERT INTO documents (id, state, updated_at) VALUES ($1,$2,NOW())
		 ON CONFLICT (id) DO UPDATE SET state=$2, updated_at=NOW()`,
		docID, state)
	return err
}

func (s *PGStore) Load(ctx context.Context, docID string) ([]byte, error) {
	var state []byte
	err := s.db.QueryRowContext(ctx,
		`SELECT state FROM documents WHERE id=$1`, docID).Scan(&state)
	if err == sql.ErrNoRows {
		return nil, nil
	}
	return state, err
}
```

## 6. 数据库设计

```sql
CREATE TABLE documents (
    id          TEXT PRIMARY KEY,
    state       BYTEA,                          -- Yjs 编码的完整文档快照
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE document_permissions (
    doc_id      TEXT NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
    user_id     TEXT NOT NULL,
    permission  TEXT NOT NULL CHECK (permission IN ('read','write','admin')),
    PRIMARY KEY (doc_id, user_id)
);
```

**持久化策略**：每 50 次操作自动打快照（`snapshotEvery = 50`），Golang 后端直接存储 `Y.encodeStateAsUpdate` 的二进制数据。

## 7. 部署

### Docker Compose

```yaml
services:
  collab:
    build: ./collab-server
    ports: ["8080:8080"]
    environment:
      - DATABASE_URL=postgres://postgres:secret@db/docs?sslmode=disable
      - REDIS_URL=redis:6379
      - JWT_SECRET=your-secret
    depends_on: [db, redis]

  db:
    image: postgres:16-alpine
    environment: { POSTGRES_DB: docs, POSTGRES_PASSWORD: secret }
    volumes: [pgdata:/var/lib/postgresql/data]

  redis:
    image: redis:7-alpine

  nginx:
    image: nginx:alpine
    ports: ["80:80"]
    volumes: [./nginx.conf:/etc/nginx/nginx.conf]

volumes:
  pgdata:
```

### Nginx 配置

```nginx
upstream collab {
    hash $arg_doc consistent;    # 同一文档路由到同一实例
    server collab_1:8080;
    server collab_2:8080;
}

server {
    listen 80;

    location /ws {
        proxy_pass http://collab;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
    }
}
```

## 8. 性能优化

| 优化点 | 方案 |
|--------|------|
| 操作压缩 | 前端 debounce 50ms 合并连续输入 |
| 快照间隔 | 每 50 次操作自动持久化 |
| 离线编辑 | `y-indexeddb` 本地持久化，重连自动合并 |
| 多实例 | Redis Pub/Sub 跨实例广播 |
| 连接管理 | ping/pong 心跳 + 1MB 消息上限 |
| 负载均衡 | Nginx 一致性哈希，同一文档路由到同一实例 |
| Tombstone GC | 定期调用 `Y.encodeStateAsUpdate` 合并，丢弃历史操作 |

## 9. 选型对比

| 场景 | 推荐方案 |
|------|----------|
| Markdown 协作（本文方案） | CodeMirror 6 + Yjs + Golang |
| 富文本协作 | TipTap + Yjs + Hocuspocus (Node.js) |
| 企业级高并发 | OT + 自研操作转换服务 |
| 快速 MVP | CodeMirror + Yjs + y-websocket 官方服务 |
| JSON 文档协作 | Automerge + 自研后端 |
