# 智能体开发框架

---

## 一、AutoGen

### 1.1 简介

AutoGen 是微软开源的多智能体框架，支持 Agent 间对话协作。

### 1.2 核心概念

| 概念 | 说明 |
|------|------|
| AssistantAgent | AI 助手，基于 LLM |
| UserProxyAgent | 用户代理，代表人类 |
| GroupChat | 多 Agent 群聊 |
| ConversableAgent | 可对话的 Agent 基类 |

### 1.3 基本使用

```python
import autogen

config_list = [
    {"model": "gpt-4", "api_key": "your-key"}
]

llm_config = {
    "config_list": config_list,
    "temperature": 0,
}

assistant = autogen.AssistantAgent(
    name="assistant",
    llm_config=llm_config,
)

user_proxy = autogen.UserProxyAgent(
    name="user",
    human_input_mode="NEVER",
    max_consecutive_auto_reply=3,
    code_execution_config={"work_dir": "coding"},
)

user_proxy.initiate_chat(
    assistant,
    message="写一个 Python 函数计算斐波那契数列",
)
```

### 1.4 多 Agent 协作

```python
engineer = autogen.AssistantAgent(
    name="engineer",
    system_message="你是一个资深工程师，负责编写代码",
    llm_config=llm_config,
)

reviewer = autogen.AssistantAgent(
    name="reviewer",
    system_message="你是一个代码审查专家，负责审查代码质量和安全性",
    llm_config=llm_config,
)

groupchat = autogen.GroupChat(
    agents=[user_proxy, engineer, reviewer],
    messages=[],
    max_round=10,
)

manager = autogen.GroupChatManager(
    groupchat=groupchat,
    llm_config=llm_config,
)

user_proxy.initiate_chat(
    manager,
    message="实现一个 HTTP 服务器",
)
```

---

## 二、CrewAI

### 2.1 简介

CrewAI 是角色扮演式多智能体框架，通过定义 Agent 角色、目标和工具来编排任务。

### 2.2 核心概念

| 概念 | 说明 |
|------|------|
| Agent | 角色，有目标、背景故事、工具 |
| Task | 任务，有描述、预期输出 |
| Crew | 团队，编排 Agent 执行任务 |
| Process | 流程：顺序 / 层级 |

### 2.3 基本使用

```python
from crewai import Agent, Task, Crew, Process
from crewai_tools import SerperDevTool, ScrapeWebsiteTool

search_tool = SerperDevTool()
scrape_tool = ScrapeWebsiteTool()

researcher = Agent(
    role="技术研究员",
    goal="研究最新 AI 技术趋势",
    backstory="你是一个经验丰富的技术研究员，擅长分析和总结",
    tools=[search_tool, scrape_tool],
    verbose=True,
)

writer = Agent(
    role="技术作家",
    goal="撰写高质量的技术文章",
    backstory="你是一个专业作家，擅长将复杂技术用简单语言解释",
    verbose=True,
)

research_task = Task(
    description="研究 2024 年最重要的 AI 技术趋势",
    expected_output="包含 5 个关键趋势的详细报告",
    agent=researcher,
)

write_task = Task(
    description="基于研究报告撰写一篇 2000 字的技术文章",
    expected_output="结构清晰、语言流畅的技术文章",
    agent=writer,
)

crew = Crew(
    agents=[researcher, writer],
    tasks=[research_task, write_task],
    process=Process.sequential,
    verbose=True,
)

result = crew.kickoff()
print(result)
```

---

## 三、LangGraph

### 3.1 简介

LangGraph 是基于图的状态机多 Agent 编排框架，支持循环、分支、持久化。

### 3.2 核心概念

| 概念 | 说明 |
|------|------|
| State | 共享状态（TypedDict） |
| Node | 节点，处理状态的函数 |
| Edge | 边，定义节点间的转移 |
| Conditional Edge | 条件边，根据状态决定下一个节点 |
| Checkpoint | 检查点，支持持久化和恢复 |

### 3.3 基本使用

```python
from typing import TypedDict, Annotated
from langgraph.graph import StateGraph, END
from langgraph.graph.message import add_messages

class State(TypedDict):
    messages: Annotated[list, add_messages]
    next_agent: str

def researcher(state: State):
    response = llm.invoke(f"研究以下问题：{state['messages'][-1].content}")
    return {"messages": [response], "next_agent": "writer"}

def writer(state: State):
    response = llm.invoke(f"基于以下研究写文章：{state['messages'][-1].content}")
    return {"messages": [response], "next_agent": "reviewer"}

def reviewer(state: State):
    response = llm.invoke(f"审查以下文章：{state['messages'][-1].content}")
    return {"messages": [response]}

def should_continue(state: State):
    if state["next_agent"] == "reviewer":
        return "reviewer"
    return "writer"

graph = StateGraph(State)

graph.add_node("researcher", researcher)
graph.add_node("writer", writer)
graph.add_node("reviewer", reviewer)

graph.set_entry_point("researcher")
graph.add_conditional_edges("researcher", should_continue)
graph.add_edge("writer", "reviewer")
graph.add_edge("reviewer", END)

app = graph.compile()

result = app.invoke({"messages": [("user", "写一篇关于 RAG 的文章")]})
```

### 3.4 带工具的 Agent

```python
from langgraph.prebuilt import create_react_agent

tools = [search_tool, calculator_tool]

app = create_react_agent(llm, tools)

result = app.invoke({
    "messages": [("user", "北京天气如何？25*4等于多少？")]
})
```

---

## 四、MCP (Model Context Protocol)

### 4.1 简介

MCP 是 AI 模型与外部工具/数据的标准通信协议。

### 4.2 架构

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│   Host   │ ←→ │  Client  │ ←→ │  Server  │
│ (LLM App)│     │ (MCP客户端)│     │ (工具/数据)│
└──────────┘     └──────────┘     └──────────┘

Server 提供:
- Tools: 可调用的函数
- Resources: 可读取的数据
- Prompts: 预定义的提示词模板
```

### 4.3 MCP Server 示例

```python
from mcp.server import Server
from mcp.types import Tool, TextContent

server = Server("my-tools")

@server.list_tools()
async def list_tools():
    return [
        Tool(
            name="get_weather",
            description="获取城市天气",
            inputSchema={
                "type": "object",
                "properties": {
                    "city": {"type": "string", "description": "城市名称"}
                },
                "required": ["city"],
            },
        )
    ]

@server.call_tool()
async def call_tool(name, arguments):
    if name == "get_weather":
        city = arguments["city"]
        weather = fetch_weather(city)
        return [TextContent(type="text", text=f"{city}: {weather}")]
```

---

## 五、框架对比

| 维度 | AutoGen | CrewAI | LangGraph |
|------|---------|--------|-----------|
| 编排方式 | 对话驱动 | 角色扮演 | 图状态机 |
| 灵活性 | 中 | 中 | 高 |
| 循环/分支 | 有限 | 有限 | 完整支持 |
| 持久化 | 有限 | 有限 | Checkpoint |
| 人机协作 | 强 | 中 | 中 |
| 学习曲线 | 低 | 低 | 中 |
| 适用场景 | 代码生成/对话 | 团队协作 | 复杂工作流 |
