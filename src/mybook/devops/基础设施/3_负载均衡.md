# 负载均衡

- [1. 入门](#1-入门)
  - [1.1. Nginx-Controller](#11-nginx-controller)
    - [1.1.1. 为什么需要动态更新呢？](#111-为什么需要动态更新呢)
  - [1.2. traefik](#12-traefik)
- [2. 参考](#2-参考)

## 1. 入门

- 硬件负载均衡
  - 如 F5，性能好，但是贵。一般的互联网公司都没有采集硬件负载均衡
- 软件负载均衡
  - 4 层：典型的如 LVS
  - 7 层：典型的如 Nginx、HAProxy
    - 目前这两个都可以实现 4 层，但是更多的还是使用 Nginx 的 7 层功能。
- 典型的负载均衡方案
  - client -> LVS(4 层) -> Nginx(7层) -> server
    - nginx upstream
- k8s负载均衡
  - kube-proxy 集群内置的内部负载均衡解决方案
  - nginx-ingress-controller方案
  - 云厂商Cloud provider
  - 自建lb方案

### 1.1. Nginx-Controller

- Nginx-Controller 就是来动态发现 Pod，然后渲染为 nginx 的 upstream；Nginx-Controller 就是一个 Nginx 再加上一个 Controller（发现 Pod 并渲染为 upstream）。
- client -> CDN -> LVS -> Nginx-Ingress-Controller -> Pod

nginx-ingress的大概流程

- 初始化
  - 初始化任务队列，n.syncIngress为任务Handler
  - 监控nginx.tmpl模板文件的变化
  - 创建新任务用来重新生成nginx.conf
- 启动
  - 通过Informer机制同步并监测k8s资源的变化
  - 给nginx进程设置单独的pgid，防止收到发给controller进程的SIGTERM信号而退出。
  - 启动nginx进程
  - 启动任务队列：用来处理List/Watch后的资源
  - 初次启动，需要触发一起配置文件同步
  - 发生错误时，临时文件暂存5分钟
- 资源监控
  - 控制器监听了Ingress、IngressClass、Endpoint、Secret、ConfigMap、Service 六种资源。
  - 定义各个资源变化的处理函数：这些处理函数实际就是解析各个资源信息，然后更新nginx配置信息
  - 监听到资源变化后生成Event并通过updateCh通道发送出去。
- 配置更新
  - 从缓存中获取Ingresses
  - 解析ingress
  - 同当前运行的配置作比对判断配置是否有改变
  - 有改变则进行如下动作
  - 判断是否能够通过lua动态更新
  - 不能通过lua动态更新，则生成新的nginx.conf并eload
  - 通过lua动态更新配置

#### 1.1.1. 为什么需要动态更新呢？

- 因为在一个kubernetes集群里service的endpoints可能会频率变动，如果都静态写进nginx.conf配置文件就意味着每次endpoints变动都要nginx -s reload 重新加载配置文件，在高并发下可能会导致某些请求时延变长，所以就需要借助lua-nginx-module使用lua来动态更新upstream上游节点。

### 1.2. traefik

K3s 中启用 Traefik Dashboard

`kubectl -n kube-system port-forward $(kubectl -n kube-system get pods --selector "app.kubernetes.io/name=traefik" --output=name) 9000:9000 --address 0.0.0.0`

`http://192.168.1.21:9000/dashboard/#/`

## 2. 参考

> [https://cloud.tencent.com/developer/article/2314178](https://cloud.tencent.com/developer/article/2314178)
>

---

## 3. LVS（Linux Virtual Server）

### 3.1 三种模式

| 模式 | 原理 | 性能 | 限制 |
|------|------|------|------|
| NAT | 请求和响应都经过 Director | 中 | Director 是瓶颈 |
| DR（Direct Routing） | 请求经 Director，响应直接返回客户端 | 高 | RS 和 Director 同网段 |
| TUN（IP Tunneling） | IP 隧道封装，RS 可跨网段 | 高 | RS 需支持 IP 隧道 |

### 3.2 IPVS 配置

```bash
ipvsadm -A -t 192.168.1.100:80 -s wrr          # 创建虚拟服务
ipvsadm -a -t 192.168.1.100:80 -r 10.0.0.1:80 -g -w 3  # DR模式添加RS
ipvsadm -a -t 192.168.1.100:80 -r 10.0.0.2:80 -g -w 2

ipvsadm -Ln                                       # 查看规则
ipvsadm -Ln --stats                               # 查看统计
ipvsadm -d -t 192.168.1.100:80 -r 10.0.0.1:80    # 删除RS
ipvsadm -D -t 192.168.1.100:80                    # 删除虚拟服务
```

### 3.3 调度算法

| 算法 | 说明 |
|------|------|
| rr | 轮询 |
| wrr | 加权轮询 |
| lc | 最少连接 |
| wlc | 加权最少连接（推荐） |
| sh | 源地址哈希 |
| dh | 目标地址哈希 |
| lblc | 基于局部的最少连接 |
| sed | 最短期望延迟 |

## 4. HAProxy

### 4.1 安装与配置

```bash
apt install haproxy
# 或
yum install haproxy
```

```cfg
# /etc/haproxy/haproxy.cfg
global
    log /dev/log local0
    maxconn 4096
    daemon

defaults
    log     global
    mode    http
    option  httplog
    option  dontlognull
    timeout connect 5s
    timeout client  50s
    timeout server  50s
    retries 3

frontend http-in
    bind *:80
    bind *:443 ssl crt /etc/haproxy/ssl/example.pem

    acl is_api path_beg /api
    acl is_static path_beg /static

    use_backend api_servers if is_api
    use_backend static_servers if is_static
    default_backend web_servers

backend web_servers
    balance roundrobin
    option httpchk GET /health
    server web1 10.0.0.1:8080 check inter 5s fall 3 rise 2
    server web2 10.0.0.2:8080 check inter 5s fall 3 rise 2

backend api_servers
    balance leastconn
    option httpchk GET /api/health
    server api1 10.0.0.3:9090 check
    server api2 10.0.0.4:9090 check

backend static_servers
    balance roundrobin
    server static1 10.0.0.5:80 check

listen stats
    bind *:8404
    mode http
    stats enable
    stats uri /stats
    stats refresh 10s
    stats admin if LOCALHOST
```

### 4.2 四层/七层代理

```cfg
# 四层（TCP）
frontend mysql_front
    bind *:3306
    mode tcp
    default_backend mysql_back

backend mysql_back
    mode tcp
    balance leastconn
    server mysql1 10.0.0.10:3306 check
    server mysql2 10.0.0.11:3306 check backup

# 七层（HTTP）
frontend http_front
    bind *:80
    mode http
    default_backend http_back
```

### 4.3 负载均衡算法

| 算法 | 说明 |
|------|------|
| roundrobin | 轮询 |
| static-rr | 静态轮询（不支持运行时调整权重） |
| leastconn | 最少连接 |
| source | 源地址哈希 |
| uri | URI 哈希 |

## 5. Nginx

### 5.1 反向代理与负载均衡

```nginx
upstream backend {
    least_conn;
    server 10.0.0.1:8080 weight=3 max_fails=3 fail_timeout=30s;
    server 10.0.0.2:8080 weight=2;
    server 10.0.0.3:8080 backup;

    keepalive 32;
}

server {
    listen 80;
    server_name example.com;

    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 5s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;

        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }

    location /api/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
}
```

### 5.2 性能调优

```nginx
worker_processes auto;
worker_rlimit_nofile 65535;

events {
    worker_connections 4096;
    use epoll;
    multi_accept on;
}

http {
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;

    keepalive_timeout 65;
    keepalive_requests 10000;

    gzip on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript;

    open_file_cache max=10000 inactive=20s;
    open_file_cache_valid 30s;
    open_file_cache_min_uses 2;

    client_max_body_size 50m;
    client_body_buffer_size 128k;
}
```

### 5.3 安全配置

```nginx
server {
    listen 443 ssl http2;
    server_name example.com;

    ssl_certificate /etc/nginx/ssl/example.crt;
    ssl_certificate_key /etc/nginx/ssl/example.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        proxy_pass http://backend;
    }
}
```

## 6. Keepalived

### 6.1 VRRP 协议

Keepalived 基于 VRRP（虚拟路由冗余协议）实现高可用：
- 多台路由器组成虚拟路由组
- 一台 Master，多台 Backup
- 共享虚拟 IP（VIP）
- Master 定期发送 VRRP 通告
- Backup 超时未收到通告则选举新 Master

### 6.2 配置

```cfg
# /etc/keepalived/keepalived.conf - Master
global_defs {
    router_id LVS_MASTER
}

vrrp_script chk_nginx {
    script "/etc/keepalived/check_nginx.sh"
    interval 2
    weight -20
    fall 3
    rise 2
}

vrrp_instance VI_1 {
    state MASTER
    interface eth0
    virtual_router_id 51
    priority 100
    advert_int 1

    authentication {
        auth_type PASS
        auth_pass secret123
    }

    virtual_ipaddress {
        192.168.1.100/24
    }

    track_script {
        chk_nginx
    }
}
```

```cfg
# /etc/keepalived/keepalived.conf - Backup
vrrp_instance VI_1 {
    state BACKUP
    interface eth0
    virtual_router_id 51
    priority 90
    advert_int 1

    authentication {
        auth_type PASS
        auth_pass secret123
    }

    virtual_ipaddress {
        192.168.1.100/24
    }

    track_script {
        chk_nginx
    }
}
```

### 6.3 健康检查脚本

```bash
#!/bin/bash
# /etc/keepalived/check_nginx.sh
if ! pidof nginx > /dev/null; then
    systemctl start nginx
    sleep 2
    if ! pidof nginx > /dev/null; then
        exit 1
    fi
fi
exit 0
```

### 6.4 Keepalived + LVS

```cfg
virtual_server 192.168.1.100 80 {
    delay_loop 6
    lb_algo wlc
    lb_kind DR
    persistence_timeout 0
    protocol TCP

    real_server 10.0.0.1 80 {
        weight 3
        TCP_CHECK {
            connect_timeout 3
            retry 3
            delay_before_retry 3
        }
    }

    real_server 10.0.0.2 80 {
        weight 2
        TCP_CHECK {
            connect_timeout 3
            retry 3
            delay_before_retry 3
        }
    }
}
```

## 7. 架构选型对比

| 特性 | LVS | HAProxy | Nginx |
|------|-----|---------|-------|
| 工作层 | 四层（L4） | 四层+七层 | 七层（L4 有限） |
| 性能 | 极高（内核态） | 高 | 高 |
| 配置复杂度 | 高 | 中 | 低 |
| 健康检查 | 基础 | 丰富 | 基础 |
| 会话保持 | 支持 | 支持 | 支持 |
| 适用场景 | 超大规模入口 | 四七层混合 | HTTP 反向代理 |
| 典型架构 | LVS → HAProxy → Nginx | HAProxy → Nginx | Nginx 直接代理 |
