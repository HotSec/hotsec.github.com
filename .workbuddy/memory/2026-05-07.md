# 2026-05-07 工作日志

## 收集高Star FastAPI项目的CLAUDE.md/AGENTS.md

- 扫描了GitHub Top 500 FastAPI项目（`fastapi+language:Python`搜索）
- 并行HEAD请求扫描，发现**29个**含CLAUDE.md/AGENTS.md的项目
- Top项目：polarsource/polar (9794⭐)、pycaret/pycaret (9771⭐)、xerrors/Yuxi (5093⭐)
- 采用率约5.8%（29/500），高于Go项目的1.6%但低于预期
- 采集过程中遇到GitHub API rate limit（60次/小时），改用raw.githubusercontent.com绕过
- 最终内容文件因超时未完整保存到`/tmp/fastapi_final_results.json`

## 融合输出AGENTS.md通用规则报告

- 融合了06_agents/03_AI-Coding实践路径下所有合集文档的数据
- 分析72+个项目的AGENTS.md/CLAUDE.md，提炼**8大通用规则、40+条具体规则**
- 8大规则：构建/测试命令、代码风格、Commit/PR规范、项目结构、禁止事项、AI参与规范、安全要求、Skill系统
- 输出：`/Volumes/SN740/code/notebook/src/mybook/06_agents/03_AI-Coding实践/17_开源项目AGENTS.md通用规则报告.md`
- 更新了索引页 00_索引页.md

## 生成DRF后台管理系统AGENTS模板

- 基于通用Python模板(15号) + Django项目合集(11号) + Sentry/PostHog等大型Django项目模式
- 输出：`18_DRF后台管理系统AGENTS模板.md`
- 重点包含：Serializer/ViewSet规范、权限与数据隔离、Celery任务规范、API设计规范、数据库N+1防护、DRF专项反模式
- 更新了索引页

## 审查合并Django/DRF双版本AGENTS模板

- 审查了 `18_Django后台管理系统AGENTS模板.md` 和 `18_DRF后台管理系统AGENTS模板.md`
- Django版独有内容：分层架构表、RespHelper封装、自定义基类、create_user/update_user审计字段、本地开发流程
- 合并3项有价值内容到DRF版：分层架构表、审计字段规范、本地开发流程
- 其余Django版独有内容（RespHelper、ListAPIViews自定义基类）为特定项目抽象，DRF版的ViewSet+Router方案更标准，不合并
- 删除了 `18_Django后台管理系统AGENTS模板.md`，仅保留 `18_DRF后台管理系统AGENTS模板.md`

## 精简DRF AGENTS模板

- 从433行精简至约230行，削减47%
- 删除项：独立的"本地开发流程"节（命令节已覆盖）、独立的"项目概述+技术栈"双节合并、冗余代码示例
- 压缩项：反模式从通用+DRF专项17条→10条（去掉与正文规则重复的）、测试示例4方法→2方法、AI辅助开发3行并入Git规范
- 合并项：安全注意事项中与反模式重叠的条目、API响应格式示例与错误处理合并

## 二次精简DRF AGENTS模板

- 从260行精简至206行，再削减21%
- 删除独立的"反模式"节——所有禁止项已融入正文各节（分层架构表❌列、DRF规范条文中"禁止"字样、安全节末尾生产环境禁令）
- 合并"权限与数据隔离"小节入"DRF规范"（4条→2条精炼表述）
- 删除ViewSet示例后的3条重复规则（示例代码已展示）
- 命名表从7行缩至3行（Model/Serializer/ViewSet合并一行，删除Celery Task和配置变量重复条目）
- N+1代码示例改为正文条文"禁止循环中查库"
- 项目结构树精简core/和tests/的逐行展开
- Git规范补充禁止项：--no-verify / --amend已推送commit / 直推main

## 三次精简DRF AGENTS模板

- 从207行压缩至147行，达标（<150行），总削减从433行起算为66%
- 项目结构树：apps/下逐文件罗列→单行花括号展开 `{models,serializers,views,...}.py`
- 分层架构表→4行列表（每行：层级+目录+职责+禁止）
- 命名3行表→1行内联（PascalCase+后缀 | kebab-case | [app]_[verb]_[noun]）
- 错误处理JSON示例→条文形式
- ViewSet示例：删除search_fields/ordering_fields（与filterset_class重复展示），精简到14行
- Celery示例：删除User.objects.get行（合并为注释），精简到8行
- 项目概述+技术栈：从6行压缩到2行（一行描述+一行技术栈）

## 融合AGENTS示例.md通用规则到DRF模板

- 从AGENTS示例.md融合"通用原则"和"Python代码规范"到18_DRF后台管理系统AGENTS模板.md
- 新增"通用原则"节：先理解再动手、最小变更、可验证性、依赖管理（优先已有库）
- Python风格新增：函数内导入仅解决循环依赖、优先async/await避免混用、常量UPPER_SNAKE_CASE
- 错误处理新增：错误响应不暴露内部细节（堆栈/SQL/文件路径）、日志不输出敏感信息
- 安全新增：禁止eval/exec、SQL用ORM参数化禁止拼接、密码用bcrypt/argon2、JWT验证签名+过期
- 数据库新增：迁移必须幂等、新增列提供默认值、频繁查询字段加索引、写操作用事务
- API新增：409冲突/422校验失败状态码、废弃字段标记deprecated而非删除
- 测试新增：修改代码后运行已有测试确保不破坏
- Git精简：去掉重复的AI/禁止条款，保留核心
- 最终139行，控制在150行以内
